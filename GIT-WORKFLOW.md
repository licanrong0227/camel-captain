# 项目协作与多设备同步指南

本指南面向不熟悉 Git 的产品、设计、运营和文档协作人员，说明如何在「骆驼队长跨境 ERP」项目里安全地保存版本、和团队协作、异地办公以及在多台设备之间同步。

> 你不需要记命令。日常让 AI 代做同步即可：它检查状态、保存版本、推送，你只负责在需要登录授权时确认账号。下面命令只作为你自查或给其他设备操作时参考。

## 1. 核心概念（用生活化的话说）

| 概念 | 含义 |
|------|------|
| 提交（commit） | 给当前所有改动拍一张可回退的快照，附带一句人话说明 |
| 推送（push） | 把本地保存好的版本上传到远程平台，让团队和另一台设备能看到 |
| 拉取（pull） | 把远程平台上的最新版本下载到本地，跟别人/另一台设备同步 |
| 远程仓库（remote） | 托管在代码平台上的项目副本，是大家同步的中转站 |
| 冲突（conflict） | 你和别人改了同一处内容，系统不知道该选哪份，需要人决定 |

本项目远程仓库：`https://github.com/licanrong0227/camel-captain.git`（GitHub）。

## 2. 日常同步习惯（多设备/异地办公）

保持一个固定节奏，任何设备都不会丢东西：

```
开工/换设备前   →  拉取最新（pull）
改完一段工作    →  保存版本（commit）
准备下班/交接   →  推送到远程（push）
```

简化成一句话：**先拉后改，改完就推**。

- 每台设备开工第一件事先拉取，避免基于过期内容修改。
- 每次改完一批有关联的工作就保存一个版本，并写一句人能看懂的说明（例：`新增跨境物流看板原型`）。
- 推送前可以先让 AI 检查一遍：有没有误传的密码、临时文件、大文件。

## 3. 团队协作要点

- **不要覆盖别人的改动**：推送前如果提示"有人也改了同样的地方"，说明产生了冲突，这是正常现象，不是文件坏了。处理方式是先保护双方的改动，逐处决定保留哪份，AI 会帮你生成冲突内容对照，不用自己读命令输出。
- **不要强行回退历史**：除非确认要影响团队看到的历史，否则不执行"删除历史/强行覆盖远程"的操作。
- **多人分工（可选）**：如果多人同时改同一批文件容易撞车，可以让负责人开一个独立草稿（分支），改好确认后再合回正式版本。普通小团队直接"拉取→改→提交→推送"即可。
- **权限问题不是你的问题**：提示"没有权限"时，是账号授权或组织成员问题，请管理员添加协作者或授权，文件本身没坏。

## 4. 在新设备上工作（异地办公/换电脑）

1. 在新电脑上安装 Git，并登录 GitHub（用浏览器/命令行授权一次）。
2. 确保这台设备知道自己是谁（一次性设置）：
   ```
   git config --global user.name "你的名字"
   git config --global user.email "你的邮箱"
   ```
3. 把项目拿下来：向 AI 提供仓库链接 `https://github.com/licanrong0227/camel-captain.git`，AI 会帮你克隆到本地。
4. 之后就是上面第 2 节的日常节奏。换设备不会丢东西，因为版本都在远程平台上。

## 5. 安全红线

- 不要把密码、token、密钥、账号导出文件、`.env` 提交进仓库；提交前让 AI 检查一遍。
- 涉及"恢复/回退"时，先说明清楚是：撤销还没保存的修改、找回某个删掉的文件、还是整个项目回到历史版本。远程回退有风险，先确认再执行。
- 如果没把握，就把想法和需求用一句话告诉 AI："帮我把本地当前状态保存并同步到远程"即可。

## 6. 一句话总结

> 远程平台是"所有设备的共同记忆"，本地只是其中一份草稿。**先拉后改、改完就推、每天至少同步一次**，就永远不会丢工作。