# 设计系统任务计划

主题：`kuacheng-bi`（骆驼队长BI）

## 状态定义

- `待确认`：范围、来源或关键设计决策尚未确认。
- `待执行`：范围已确认，可以开始。
- `执行中`：当前正在采集、生成或同步。
- `待验收`：产物已完成，等待 ready 检查、视觉验收或用户确认。
- `已完成`：当前任务已经通过验收。
- `阻塞`：来源、工具、授权或依赖不足，暂时不能继续。

## 执行阶段

| 阶段 | 目标 | 进入条件 | 完成条件 | 状态 |
| --- | --- | --- | --- | --- |
| 一期 | 完成来源登记、标准主题产物和验收 | 目标主题与关键来源明确 | 标准主题产物一致且预览通过 | 已完成 |
| 后续 | 按确认顺序实现高频组件和模板 | 一期已完成且任务范围已确认 | 对应任务逐项通过验收 | 待确认 |

一期默认不实现组件和模板；用户明确扩大本次范围时，才把对应任务调整到当前阶段。

## 一期主题任务

| 任务 ID | 阶段 | 类别 | 任务 | 预期产物 | 来源 ID | 依赖任务 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `theme-001` | 一期 | 来源 | 来源采集与登记 | `SOURCES.md` 和本地证据 | `src-001` | 无 | 来源、边界、状态和缺口可追溯 | 已完成 |
| `theme-002` | 一期 | 规范 | 设计规范 | `DESIGN.md` | `src-001` | `theme-001` | 9 段完整，事实与推断边界清楚 | 已完成 |
| `theme-003` | 一期 | 派生 | 派生主题产物 | `theme.json`、tokens、CSS 和入口 | `src-001` | `theme-002` | 全部派生内容与 `DESIGN.md` 一致 | 已完成 |
| `theme-004` | 一期 | 验收 | 预览与验收 | ready 结果和真实预览 | `src-001` | `theme-003` | 页面可访问，视觉和资源检查通过 | 已完成 |

## 组件任务

Radix 无头组件是默认技术候选，不是强制依赖。用户要求、现有技术栈、无障碍行为或组件特性可以覆盖该默认选择。

| 任务 ID | 阶段 | 类别 | 组件 | 语义用途与范围 | 变体和状态 | 无障碍要求 | 来源 ID | 技术说明 | 依赖任务 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `component-001` | 后续 | 组件 | 管理端框架（侧边栏+顶栏+TagsView） | 中后台应用壳；含明/暗侧边栏两种形态（待确认） | collapsed 54px / expanded 140px；mobile 抽屉 | 键盘导航 + aria-expanded | `src-001` | 默认评估 Radix；管理端布局可能直接用 CSS 布局，记录采用或覆盖原因 | `theme-004` | 折叠、移动端抽屉与高亮态正确 | 待确认 |
| `component-002` | 后续 | 组件 | 数据看板卡片（metric card） | 数字指标卡、趋势徽标、渐变图标底色 | hover lift；responsive 栅格 | 对比度满足表格文本 | `src-001` | 依据登录页卡片与 dashboard CSS 变量；dashboard 实图补齐前保持待确认 | `theme-004` | 栅格重排正确 | 待确认 |

## 模板任务

| 任务 ID | 阶段 | 类别 | 模板 | 适用场景与组成区块 | 响应式要求 | 来源 ID | 依赖任务 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `template-001` | 后续 | 模板 | 登录页 | 分屏或居中卡片登录：品牌渐变背景、浮动球装饰、表单卡片 | 移动端单列收窄；输入与按钮全宽 | `src-001` | `theme-004` | 有截图对照 | 待确认 |
| `template-002` | 后续 | 模板 | 数据仪表盘 | 顶部页签、筛选区、指标卡行、图表网格、表格列表 | 卡片栅格断点重排 | `src-001` | `theme-004` | dashboard 实图补齐后核对 | 待确认 |

## 执行入口

每次执行说明包含：

- 读取当前主题的 `SOURCES.md`、`PLAN.md`、`DESIGN.md` 和标准派生文件。
- 读取当前任务列出的来源证据。
- 按依赖顺序执行，并在开始、待验收和完成时更新状态。
- 新增、移除或改变组件/模板范围时同步更新本文件。

## 一期验收备注

- `check-app-ready` 在本机返回 `spawn EINVAL`（脚本 spawn npm 的环境问题，非主题问题）；typecheck 全库报错均来自无关原型 `help-center-admin-copy`，主题文件无 TS 错误。
- 已通过直接请求验证：dev server `http://localhost:51720/themes/kuacheng-bi` 返回 200；`index.tsx`、`style.css`、`tokens.json` 与 4 张预览图资源全部 200 且可编译。
- Make 管理端信号：`http://localhost:53817/?projectId=make-project-2&theme=kuacheng-bi`（200）。